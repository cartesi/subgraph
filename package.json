{
  "name": "cartesi-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ cartesi-corp/cartesi-subgraph",
    "create-local": "graph create --node http://localhost:8020/ cartesi-corp/cartesi-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ cartesi-corp/cartesi-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cartesi-corp/cartesi-subgraph",
    "prepare:ropsten": "mustache config/ropsten.json subgraph.template.yaml > subgraph.yaml",
    "prepare:localhost": "mustache config/localhost.json subgraph.template.yaml > subgraph.yaml"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.0",
    "mustache": "^4.0.1"
  }
}
