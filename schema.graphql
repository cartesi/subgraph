type LotteryWinner @entity {
    id: ID!
    winner: Bytes!
    prize: BigInt!
    time: BigInt!
    txHash: String!
}

type LotteryTicket @entity {
    id: ID!
    count: BigInt!
    _winner: Bytes!
    _roundCount: BigInt!
}
